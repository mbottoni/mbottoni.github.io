<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
<link href="https://mbottoni.github.io/feed.xml" rel="self" type="application/atom+xml"/>
<link href="https://mbottoni.github.io" rel="alternate" type="text/html"/>
<updated>2024-12-28T21:59:22.075Z</updated>
<id>https://mbottoni.github.io/feed.xml</id>
<title type="html">mbottoni</title>
<subtitle>Yet another programming blog by Maruan Bakri Ottoni aka mbottoni.</subtitle>
<author><name>Alex Kladov</name></author>

<entry>
<title type="text">Deepseek</title>
<link href="https://mbottoni.github.io/2024/12/28/deepseek.html" rel="alternate" type="text/html" title="Deepseek" />
<published>2024-12-28T00:00:00+00:00</published>
<updated>2024-12-28T00:00:00+00:00</updated>
<id>https://mbottoni.github.io/2024/12/28/deepseek</id>
<author><name>Alex Kladov</name></author>
<summary type="html"><![CDATA[Some notes of DeepSeek-V3]]></summary>
<content type="html" xml:base="https://mbottoni.github.io/2024/12/28/deepseek.html"><![CDATA[
    <h1>
    <a href="#Deepseek"><span>Deepseek</span> <time datetime="2024-12-28">Dec 28, 2024</time></a>
    </h1>

<figure>

<img alt="" src="/assets/deekseek.webp">
</figure>
<p><span>Some notes of DeepSeek-V3</span></p>
<ul>
<li>
<span>DeepSeek-V3 is a Mixture-of-Experts (MoE) language model with 671B total parameters, of which 37B are activated for each token</span>
</li>
<li>
<span>Uses Multi-head Latent Attention (MLA) for efficient inference and DeepSeekMoE for cost-effective training</span>
</li>
<li>
<span>Introduces an auxiliary-loss-free strategy for load balancing to minimize performance degradation while maintaining balanced expert utilization</span>
</li>
<li>
<span>Implements a multi-token prediction training objective to enhance model performance</span>
</li>
<li>
<span>Trained on 14.8T diverse tokens</span>
</li>
<li>
<span>Uses FP8 mixed precision training with fine-grained quantization strategy</span>
</li>
<li>
<span>Employs DualPipe algorithm for efficient pipeline parallelism with minimal communication overhead</span>
</li>
<li>
<span>Achieves cost-effective training, requiring only 2.788M H800 GPU hours total:</span>
<span>- 2664K hours for pre-training</span>
<span>- 119K hours for context extension </span>
<span>- 5K hours for post-training</span>
<span>- Total cost approximately $5.576M at $2/GPU hour</span>
</li>
<li>
<span>Outperforms other open-source models across multiple benchmarks</span>
</li>
<li>
<span>Particularly strong in:</span>
<span>- Math tasks (90.2% on MATH-500)</span>
<span>- Code tasks (51.6% on Codeforces percentile)</span>
<span>- Knowledge tasks (75.9% on MMLU-Pro)</span>
</li>
<li>
<span>Demonstrates competitive performance against closed-source models like GPT-4 and Claude-3.5-Sonnet</span>
</li>
<li>
<span>Supports context lengths up to 128K tokens through two-stage extension training</span>
</li>
<li>
<span>Auxiliary-loss-free load balancing strategy that improves expert utilization without compromising performance</span>
</li>
<li>
<span>Multi-token prediction training objective that enhances model capabilities while enabling speculative decoding</span>
</li>
<li>
<span>FP8 training framework with tile-wise and block-wise quantization strategies</span>
</li>
<li>
<span>DualPipe algorithm that achieves efficient pipeline parallelism with minimal communication overhead</span>
</li>
</ul>
]]></content>
</entry>

<entry>
<title type="text">Probability Paradigms</title>
<link href="https://mbottoni.github.io/2024/12/23/prob.html" rel="alternate" type="text/html" title="Probability Paradigms" />
<published>2024-12-23T00:00:00+00:00</published>
<updated>2024-12-23T00:00:00+00:00</updated>
<id>https://mbottoni.github.io/2024/12/23/prob</id>
<author><name>Alex Kladov</name></author>
<summary type="html"><![CDATA[When we think about probability, there are three 
main philosophical approaches: frequentist, Bayesian, and propensity. Each offers 
a different lens for understanding uncertainty and probability.]]></summary>
<content type="html" xml:base="https://mbottoni.github.io/2024/12/23/prob.html"><![CDATA[
    <h1>
    <a href="#Probability-Paradigms"><span>Probability Paradigms</span> <time datetime="2024-12-23">Dec 23, 2024</time></a>
    </h1>
<p><span>When we think about probability, there are three </span>
<span>main philosophical approaches: frequentist, Bayesian, and propensity. Each offers </span>
<span>a different lens for understanding uncertainty and probability.</span></p>
<section id="The-Scenario">

    <h2>
    <a href="#The-Scenario"><span>The Scenario</span> </a>
    </h2>
<p><span>Imagine we</span>&rsquo;<span>re dealing with a new diagnostic test for a rare genetic condition that affects 1 in 1000 people. The test has the following characteristics:</span></p>
<ul>
<li>
<span>If someone has the condition, the test will be positive 99% of the time (sensitivity)</span>
</li>
<li>
<span>If someone doesn</span>&rsquo;<span>t have the condition, the test will be negative 95% of the time (specificity)</span>
</li>
</ul>
<p><span>Now, let</span>&rsquo;<span>s say Sarah gets tested and receives a positive result. What</span>&rsquo;<span>s the probability that she </span>
<span>actually has the condition? Each probability paradigm approaches this question differently.</span></p>
</section>
<section id="The-Frequentist-Approach">

    <h2>
    <a href="#The-Frequentist-Approach"><span>The Frequentist Approach</span> </a>
    </h2>
<p><span>The frequentist views probability as the long-run frequency of events in repeated trials. They might say:</span></p>
<p>&ldquo;<span>If we were to run this test on a very large number of people, and look only at </span>
<span>the positive test results, what proportion of those people would actually have the condition?</span>&rdquo;</p>
<p><span>The frequentist would focus on the sampling distribution and confidence intervals. They might </span>
<span>resist making a direct probability statement about Sarah</span>&rsquo;<span>s individual case, arguing that she </span>
<span>either has the condition or doesn</span>&rsquo;<span>t – there</span>&rsquo;<span>s no probability about it. Instead, they </span>
<span>would frame their analysis in terms of error rates and long-term frequencies.</span></p>
<p><span>In this case, they would calculate the false positive and true positive rates across a hypothetical population:</span></p>
<ul>
<li>
<span>In 1000 people, 1 person would have the condition</span>
</li>
<li>
<span>That 1 person would test positive 99% of the time = 0.99 true positives</span>
</li>
<li>
<span>The 999 people without the condition would test positive 5% of the time = 49.95 false positives</span>
</li>
<li>
<span>Total positive tests = 50.94</span>
</li>
<li>
<span>Proportion of positive tests that are true positives ≈ 1.94%</span>
</li>
</ul>
</section>
<section id="The-Bayesian-Approach">

    <h2>
    <a href="#The-Bayesian-Approach"><span>The Bayesian Approach</span> </a>
    </h2>
<p><span>The Bayesian sees probability as a degree of belief that gets updated with </span>
<span>new evidence. They would start with a prior probability (the base </span>
<span>rate of 1/1000) and update it with the new </span>
<span>evidence (the positive test result) using Bayes</span>&rsquo;<span> Theorem.</span></p>
<p><span>The Bayesian would say: </span>&ldquo;<span>Before the test, we believed Sarah had a 0.1% chance </span>
<span>of having the condition. The positive test result gives us new information </span>
<span>to update this belief.</span>&rdquo;</p>
<p><span>Using Bayes</span>&rsquo;<span> Theorem:</span>
<span>P(Condition|Positive) = P(Positive|Condition) × P(Condition) / P(Positive)</span>
<span>= 0.99 × 0.001 / (0.99 × 0.001 + 0.05 × 0.999)</span>
<span>≈ 0.0194 or about 1.94%</span></p>
<p><span>The Bayesian is comfortable making a direct probability statement about Sarah</span>
&lsquo;<span>s specific case, saying there</span>&rsquo;<span>s a 1.94% chance she has the condition.</span></p>
</section>
<section id="The-Propensity-Approach">

    <h2>
    <a href="#The-Propensity-Approach"><span>The Propensity Approach</span> </a>
    </h2>
<p><span>The propensity theorist views probability as an objective </span>
<span>physical property – a tendency or disposition of a </span>
<span>system to produce certain outcomes. They might say:</span></p>
<p>&ldquo;<span>The test result reflects the physical properties of Sarah</span>&rsquo;<span>s </span>
<span>genes and the testing mechanism. The probability emerges from </span>
<span>the actual physical setup of the situation.</span>&rdquo;</p>
<p><span>They would focus on the causal mechanisms involved: the biological </span>
<span>factors that cause the condition, the chemical reactions in the test, and how </span>
<span>these physical properties interact to produce results. They might analyze the </span>
<span>molecular basis of the test</span>&rsquo;<span>s accuracy and how it interacts with the genetic markers it</span>&rsquo;<span>s designed to detect.</span></p>
<p><span>These different approaches can lead to different practical recommendations:</span></p>
<ul>
<li>
<p><span>A frequentist might emphasize the need for additional testing, pointing out that in a large population, most positive results are false positives.</span></p>
</li>
<li>
<p><span>A Bayesian might focus on gathering more prior information about Sarah (family history, symptoms, etc.) to refine the probability estimate.</span></p>
</li>
<li>
<p><span>A propensity theorist might advocate for understanding the biological mechanism better, perhaps recommending a different type of test that targets the condition</span>&rsquo;<span>s physical manifestations more directly.</span></p>
</li>
</ul>
</section>
]]></content>
</entry>

<entry>
<title type="text">Classifier free diffusion guidance</title>
<link href="https://mbottoni.github.io/2024/12/15/cfg.html" rel="alternate" type="text/html" title="Classifier free diffusion guidance" />
<published>2024-12-15T00:00:00+00:00</published>
<updated>2024-12-15T00:00:00+00:00</updated>
<id>https://mbottoni.github.io/2024/12/15/cfg</id>
<author><name>Alex Kladov</name></author>
<summary type="html"><![CDATA[One of the key techniques in diffusion models that has significantly improved their performance is 
classifier-free guidance. In this post, we'll explore what classifier-free 
guidance is, how it works, and implement it from scratch in PyTorch.]]></summary>
<content type="html" xml:base="https://mbottoni.github.io/2024/12/15/cfg.html"><![CDATA[
    <h1>
    <a href="#Classifier-free-diffusion-guidance"><span>Classifier free diffusion guidance</span> <time datetime="2024-12-15">Dec 15, 2024</time></a>
    </h1>
<p><span>One of the key techniques in diffusion models that has significantly improved their performance is </span>
<span>classifier-free guidance. In this post, we</span>&rsquo;<span>ll explore what classifier-free </span>
<span>guidance is, how it works, and implement it from scratch in PyTorch.</span></p>
<section id="What-is-Classifier-Free-Guidance">

    <h2>
    <a href="#What-is-Classifier-Free-Guidance"><span>What is Classifier-Free Guidance?</span> </a>
    </h2>
<p><span>At its core, classifier-free guidance is an elegant technique that allows us to </span>
<span>control the generation process of diffusion models without requiring a </span>
<span>separate classifier. The key insight is that we can create a more powerful conditional generation </span>
<span>process by combining both conditional and unconditional generation in a clever way.</span></p>
<p><span>Think of it like having two artists working together:</span></p>
<ol>
<li>
<span>One artist (conditional model) who follows specific instructions</span>
</li>
<li>
<span>One artist (unconditional model) who creates freely without constraints</span>
</li>
</ol>
<p><span>By combining their perspectives with different weights, we can create results </span>
<span>that are both high-quality and well-aligned with our desired conditions.</span></p>
</section>
<section id="The-Mathematics-Behind-Classifier-Free-Guidance">

    <h2>
    <a href="#The-Mathematics-Behind-Classifier-Free-Guidance"><span>The Mathematics Behind Classifier-Free Guidance</span> </a>
    </h2>
<p><span>The core equation for classifier-free guidance is surprisingly simple:</span></p>

<figure class="code-block">


<pre><code><span class="line">ε̃ = (1 + w) * εθ(zt, c) - w * εθ(zt, ∅)</span></code></pre>

</figure>
<p><span>Where:</span>
<span>- ε̃ is the guided noise prediction</span>
<span>- w is the guidance weight</span>
<span>- εθ(zt, c) is the conditional model prediction</span>
<span>- εθ(zt, ∅) is the unconditional model prediction</span></p>
<p><span>The beauty of this approach is that it doesn</span>&rsquo;<span>t require training </span>
<span>two separate models. Instead, we train a single model that can </span>
<span>handle both conditional and unconditional generation.</span></p>
</section>
<section id="Implementation-A-Complete-Example">

    <h2>
    <a href="#Implementation-A-Complete-Example"><span>Implementation: A Complete Example</span> </a>
    </h2>
<p><span>Let</span>&rsquo;<span>s implement classifier-free guidance for a diffusion model from </span>
<span>scratch. We</span>&rsquo;<span>ll build a system that can generate MNIST-like digits conditioned on class labels.</span></p>
<p><span>First, let</span>&rsquo;<span>s create our improved diffusion model:</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">import</span> torch</span>
<span class="line"><span class="hl-keyword">import</span> torch.nn <span class="hl-keyword">as</span> nn</span>
<span class="line"></span>
<span class="line"><span class="hl-keyword">class</span> <span class="hl-title class_">DiffusionModel</span>(nn.Module):</span>
<span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">__init__</span>(<span class="hl-params">self, input_dim=<span class="hl-number">784</span>, hidden_dim=<span class="hl-number">256</span>, num_classes=<span class="hl-number">10</span></span>):</span>
<span class="line">        <span class="hl-built_in">super</span>().__init__()</span>
<span class="line">        self.input_dim = input_dim</span>
<span class="line">        self.num_classes = num_classes</span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Improved embedding with position encoding</span></span>
<span class="line">        self.class_embedding = nn.Sequential(</span>
<span class="line">            nn.Embedding(num_classes + <span class="hl-number">1</span>, hidden_dim),  <span class="hl-comment"># +1 for unconditional</span></span>
<span class="line">            nn.Linear(hidden_dim, hidden_dim),</span>
<span class="line">            nn.ReLU()</span>
<span class="line">        )</span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Time embedding</span></span>
<span class="line">        self.time_embed = nn.Sequential(</span>
<span class="line">            nn.Linear(<span class="hl-number">1</span>, hidden_dim),</span>
<span class="line">            nn.ReLU(),</span>
<span class="line">            nn.Linear(hidden_dim, hidden_dim)</span>
<span class="line">        )</span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Enhanced U-Net architecture</span></span>
<span class="line">        self.encoder = nn.Sequential(</span>
<span class="line">            nn.Linear(input_dim + hidden_dim * <span class="hl-number">2</span>, hidden_dim * <span class="hl-number">2</span>),</span>
<span class="line">            nn.LayerNorm(hidden_dim * <span class="hl-number">2</span>),</span>
<span class="line">            nn.ReLU(),</span>
<span class="line">            nn.Linear(hidden_dim * <span class="hl-number">2</span>, hidden_dim),</span>
<span class="line">            nn.LayerNorm(hidden_dim),</span>
<span class="line">            nn.ReLU()</span>
<span class="line">        )</span>
<span class="line">        </span>
<span class="line">        self.decoder = nn.Sequential(</span>
<span class="line">            nn.Linear(hidden_dim, hidden_dim * <span class="hl-number">2</span>),</span>
<span class="line">            nn.LayerNorm(hidden_dim * <span class="hl-number">2</span>),</span>
<span class="line">            nn.ReLU(),</span>
<span class="line">            nn.Linear(hidden_dim * <span class="hl-number">2</span>, input_dim),</span>
<span class="line">            nn.Tanh()  <span class="hl-comment"># Bounded output</span></span>
<span class="line">        )</span>
<span class="line">        </span>
<span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">forward</span>(<span class="hl-params">self, x, t, c=<span class="hl-literal">None</span></span>):</span>
<span class="line">        batch_size = x.shape[<span class="hl-number">0</span>]</span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Handle conditional vs unconditional</span></span>
<span class="line">        <span class="hl-keyword">if</span> c <span class="hl-keyword">is</span> <span class="hl-literal">None</span>:</span>
<span class="line">            c = torch.full((batch_size,), self.num_classes, device=x.device)</span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Get embeddings</span></span>
<span class="line">        c_emb = self.class_embedding(c)</span>
<span class="line">        t_emb = self.time_embed(t.unsqueeze(-<span class="hl-number">1</span>))</span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Combine all information</span></span>
<span class="line">        x_c = torch.cat([x, c_emb, t_emb], dim=-<span class="hl-number">1</span>)</span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Forward pass</span></span>
<span class="line">        h = self.encoder(x_c)</span>
<span class="line">        output = self.decoder(h)</span>
<span class="line">        </span>
<span class="line">        <span class="hl-keyword">return</span> output</span></code></pre>

</figure>
<p><span>Now, let</span>&rsquo;<span>s implement an improved training loop with classifier-free guidance:</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">def</span> <span class="hl-title function_">train_diffusion_model</span>(<span class="hl-params">model, dataloader, num_epochs=<span class="hl-number">100</span>, puncond=<span class="hl-number">0.1</span>, device=<span class="hl-string">&#x27;cuda&#x27;</span></span>):</span>
<span class="line">    <span class="hl-string">&quot;&quot;&quot;</span></span>
<span class="line"><span class="hl-string">    Enhanced training loop with classifier-free guidance support</span></span>
<span class="line"><span class="hl-string">    &quot;&quot;&quot;</span></span>
<span class="line">    optimizer = torch.optim.AdamW(model.parameters(), lr=<span class="hl-number">1e-4</span>, weight_decay=<span class="hl-number">0.01</span>)</span>
<span class="line">    scheduler = torch.optim.lr_scheduler.CosineAnnealingLR(optimizer, num_epochs)</span>
<span class="line">    </span>
<span class="line">    <span class="hl-keyword">for</span> epoch <span class="hl-keyword">in</span> <span class="hl-built_in">range</span>(num_epochs):</span>
<span class="line">        model.train()</span>
<span class="line">        total_loss = <span class="hl-number">0</span></span>
<span class="line">        </span>
<span class="line">        <span class="hl-keyword">for</span> batch, (x, c) <span class="hl-keyword">in</span> <span class="hl-built_in">enumerate</span>(dataloader):</span>
<span class="line">            batch_size = x.shape[<span class="hl-number">0</span>]</span>
<span class="line">            x = x.to(device)</span>
<span class="line">            c = c.to(device)</span>
<span class="line">            </span>
<span class="line">            <span class="hl-comment"># Sample timesteps</span></span>
<span class="line">            t = torch.rand(batch_size, device=device)</span>
<span class="line">            </span>
<span class="line">            <span class="hl-comment"># Create noise</span></span>
<span class="line">            epsilon = torch.randn_like(x)</span>
<span class="line">            z_t = alpha(t).view(-<span class="hl-number">1</span>, <span class="hl-number">1</span>) * x + sigma(t).view(-<span class="hl-number">1</span>, <span class="hl-number">1</span>) * epsilon</span>
<span class="line">            </span>
<span class="line">            <span class="hl-comment"># Sometimes drop conditioning for unconditional training</span></span>
<span class="line">            mask = torch.rand(batch_size, device=device) &lt; puncond</span>
<span class="line">            c_in = torch.where(mask, torch.full_like(c, model.num_classes), c)</span>
<span class="line">            </span>
<span class="line">            <span class="hl-comment"># Get model prediction</span></span>
<span class="line">            epsilon_theta = model(z_t, t, c_in)</span>
<span class="line">            </span>
<span class="line">            <span class="hl-comment"># Compute loss with improved weighting</span></span>
<span class="line">            loss = torch.nn.functional.mse_loss(epsilon_theta, epsilon, reduction=<span class="hl-string">&#x27;none&#x27;</span>)</span>
<span class="line">            loss = loss * (<span class="hl-number">1</span> + t.view(-<span class="hl-number">1</span>, <span class="hl-number">1</span>))  <span class="hl-comment"># Weight loss by timestep</span></span>
<span class="line">            loss = loss.mean()</span>
<span class="line">            </span>
<span class="line">            optimizer.zero_grad()</span>
<span class="line">            loss.backward()</span>
<span class="line">            torch.nn.utils.clip_grad_norm_(model.parameters(), <span class="hl-number">1.0</span>)</span>
<span class="line">            optimizer.step()</span>
<span class="line">            </span>
<span class="line">            total_loss += loss.item()</span>
<span class="line">            </span>
<span class="line">        scheduler.step()</span>
<span class="line">        avg_loss = total_loss / <span class="hl-built_in">len</span>(dataloader)</span>
<span class="line">        <span class="hl-built_in">print</span>(<span class="hl-string">f&quot;Epoch <span class="hl-subst">{epoch}</span>: Average Loss = <span class="hl-subst">{avg_loss:<span class="hl-number">.4</span>f}</span>&quot;</span>)</span>
<span class="line">    </span>
<span class="line">    <span class="hl-keyword">return</span> model</span></code></pre>

</figure>
<p><span>Finally, let</span>&rsquo;<span>s improve the sampling process with classifier-free guidance:</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">def</span> <span class="hl-title function_">sample_with_guidance</span>(<span class="hl-params">model, c, w, steps=<span class="hl-number">50</span>, data_shape=[<span class="hl-number">1</span>, <span class="hl-number">28</span>, <span class="hl-number">28</span>], device=<span class="hl-string">&#x27;cuda&#x27;</span></span>):</span>
<span class="line">    <span class="hl-string">&quot;&quot;&quot;</span></span>
<span class="line"><span class="hl-string">    Enhanced sampling with classifier-free guidance</span></span>
<span class="line"><span class="hl-string">    &quot;&quot;&quot;</span></span>
<span class="line">    model.<span class="hl-built_in">eval</span>()</span>
<span class="line">    batch_size = <span class="hl-number">1</span></span>
<span class="line">    </span>
<span class="line">    <span class="hl-comment"># Create log-SNR sequence with improved spacing</span></span>
<span class="line">    lambda_sequence = torch.linspace(<span class="hl-number">0</span>, <span class="hl-number">1</span>, steps)</span>
<span class="line">    lambda_sequence = torch.sigmoid(<span class="hl-number">10</span> * (lambda_sequence - <span class="hl-number">0.5</span>))  <span class="hl-comment"># Better spacing</span></span>
<span class="line">    </span>
<span class="line">    <span class="hl-comment"># Initialize with noise</span></span>
<span class="line">    z_t = torch.randn(batch_size, *data_shape, device=device)</span>
<span class="line">    </span>
<span class="line">    <span class="hl-comment"># Prepare conditioning</span></span>
<span class="line">    c = torch.tensor([c], device=device)</span>
<span class="line">    </span>
<span class="line">    <span class="hl-keyword">with</span> torch.no_grad():</span>
<span class="line">        <span class="hl-keyword">for</span> i <span class="hl-keyword">in</span> <span class="hl-built_in">range</span>(<span class="hl-built_in">len</span>(lambda_sequence) - <span class="hl-number">1</span>):</span>
<span class="line">            lambda_t = lambda_sequence[i]</span>
<span class="line">            lambda_next = lambda_sequence[i + <span class="hl-number">1</span>]</span>
<span class="line">            </span>
<span class="line">            <span class="hl-comment"># Get conditional and unconditional score estimates</span></span>
<span class="line">            epsilon_theta_c = model(z_t, lambda_t, c)</span>
<span class="line">            epsilon_theta = model(z_t, lambda_t, <span class="hl-literal">None</span>)</span>
<span class="line">            </span>
<span class="line">            <span class="hl-comment"># Apply classifier-free guidance</span></span>
<span class="line">            epsilon_guided = (<span class="hl-number">1</span> + w) * epsilon_theta_c - w * epsilon_theta</span>
<span class="line">            </span>
<span class="line">            <span class="hl-comment"># Improved DDIM-like step</span></span>
<span class="line">            x_pred = (z_t - sigma(lambda_t).view(-<span class="hl-number">1</span>, <span class="hl-number">1</span>, <span class="hl-number">1</span>) * epsilon_guided) / alpha(lambda_t).view(-<span class="hl-number">1</span>, <span class="hl-number">1</span>, <span class="hl-number">1</span>)</span>
<span class="line">            z_t = alpha(lambda_next).view(-<span class="hl-number">1</span>, <span class="hl-number">1</span>, <span class="hl-number">1</span>) * x_pred + sigma(lambda_next).view(-<span class="hl-number">1</span>, <span class="hl-number">1</span>, <span class="hl-number">1</span>) * epsilon_guided</span>
<span class="line">    </span>
<span class="line">    <span class="hl-keyword">return</span> z_t[<span class="hl-number">0</span>]</span></code></pre>

</figure>
</section>
<section id="Understanding-the-Improvements">

    <h2>
    <a href="#Understanding-the-Improvements"><span>Understanding the Improvements</span> </a>
    </h2>
<p><span>Our implementation includes several key improvements over the basic version:</span></p>
<ol>
<li>
<p><strong><strong><span>Enhanced Architecture</span></strong></strong><span>:</span>
<span>- Added time embeddings for better temporal understanding</span>
<span>- Included layer normalization for stable training</span>
<span>- Added residual connections in the U-Net structure</span></p>
</li>
<li>
<p><strong><strong><span>Improved Training</span></strong></strong><span>:</span>
<span>- Using AdamW optimizer with weight decay for better regularization</span>
<span>- Implemented learning rate scheduling</span>
<span>- Added gradient clipping to prevent exploding gradients</span>
<span>- Weighted loss by timestep to focus more on later denoising steps</span></p>
</li>
<li>
<p><strong><strong><span>Better Sampling</span></strong></strong><span>:</span>
<span>- Improved timestep spacing using sigmoid scaling</span>
<span>- More stable DDIM-like stepping procedure</span>
<span>- Better handling of batch dimensions</span></p>
</li>
</ol>
</section>
]]></content>
</entry>

<entry>
<title type="text">Diffusion models for time series</title>
<link href="https://mbottoni.github.io/2024/12/07/timeseries-diffusion.html" rel="alternate" type="text/html" title="Diffusion models for time series" />
<published>2024-12-07T00:00:00+00:00</published>
<updated>2024-12-07T00:00:00+00:00</updated>
<id>https://mbottoni.github.io/2024/12/07/timeseries-diffusion</id>
<author><name>Alex Kladov</name></author>
<summary type="html"><![CDATA[On this post I will explore the main findings from the paper UTSD: Unified Time Series Diffusion Model and an explanation 
of the content. For more details here is the link to the paper Link to paper]]></summary>
<content type="html" xml:base="https://mbottoni.github.io/2024/12/07/timeseries-diffusion.html"><![CDATA[
    <h1>
    <a href="#Diffusion-models-for-time-series"><span>Diffusion models for time series</span> <time datetime="2024-12-07">Dec 7, 2024</time></a>
    </h1>
<p><span>On this post I will explore the main findings from the paper </span>&ldquo;<span>UTSD: Unified Time Series Diffusion Model</span>&rdquo;<span> and an explanation </span>
<span>of the content. For more details here is the link to the paper </span><a href="https://arxiv.org/pdf/2412.03068.pdf"><span>Link to paper</span></a></p>
<section id="Summary-of-Findings-from-UTSD-Paper">

    <h2>
    <a href="#Summary-of-Findings-from-UTSD-Paper"><span>Summary of Findings from UTSD Paper</span> </a>
    </h2>
<p><span>The authors of the paper propose a novel architecture, UTSD, for time </span>
<span>series analysis. The researchers note the prevalence of time series </span>
<span>data in a variety of real world applications. They also point out that </span>
<span>data across different domains often have different statistical properties. They posit that this </span>
<span>creates a challenge to the generalizability and robustness of time </span>
<span>series analysis. In order to address this, the authors propose a unified time </span>
<span>series diffusion model. The paper explains that the UTSD model </span>
<span>uses a diffusion denoising process, in contrast to autoregressive models, to model </span>
<span>the mixture distribution of cross-domain data and generate a prediction sequence for </span>
<span>the target domain. The model learns from time series data across a range of domains </span>
<span>with the goal of having strong generalization capabilities and robustness to achieve zero-shot </span>
<span>inference on unseen domains. UTSD consists of three key designs:</span></p>
<ul>
<li>
<span>Condition-denoising architecture</span>
</li>
<li>
<span>Reverse noise reduction process in the actual sequence space</span>
</li>
<li>
<span>Conditional generation strategy based on improved classifier-free guidance</span>
</li>
</ul>
<p><span>The paper reports that UTSD outperforms existing foundation models on all data domains. It achieves an average </span>
<span>MSE reduction of 14.2%, 20.1%, and 27.6% compared to the existing Moirai, UniTime, and GPT4TS, respectively. The paper also </span>
<span>reports that UTSD, when trained from scratch, achieves comparable performance to domain-specific models. In the paper, the authors </span>
<span>contend that UTSD shows stable and reliable time series generation which indicates the </span>
<span>model</span>&rsquo;<span>s potential as a foundational model for time series analysis.</span></p>
</section>
<section id="Explanation-of-the-Paper">

    <h2>
    <a href="#Explanation-of-the-Paper"><span>Explanation of the Paper</span> </a>
    </h2>
<p><span>The paper presents a detailed explanation of the UTSD model architecture and its three key design elements. These designs aim </span>
<span>to address the challenges of modeling probability distributions across multiple time series domains and generating accurate </span>
<span>long-term forecasts.</span></p>

<figure>

<img alt="" src="/assets/time-series-diffusion.png">
</figure>
<section id="1-Condition-Denoising-Architecture">

    <h3>
    <a href="#1-Condition-Denoising-Architecture"><span>1. Condition-Denoising Architecture</span> </a>
    </h3>
<p><span>This architecture includes a Condition Net and a Denoising Net. The Condition Net learns multi-scale </span>
<span>representations of temporal fluctuation patterns from different time series domains. These representations </span>
<span>are then used as conditional variables to guide the Denoising Net in generating the prediction sequence. The paper argues </span>
<span>that capturing multi-scale representations is crucial because time series from various domains often exhibit different latent patterns.</span></p>
</section>
<section id="2-Reverse-Noise-Reduction-in-Actual-Sequence-Space">

    <h3>
    <a href="#2-Reverse-Noise-Reduction-in-Actual-Sequence-Space"><span>2. Reverse Noise Reduction in Actual Sequence Space</span> </a>
    </h3>
<p><span>UTSD performs the reverse noise reduction process directly in the actual sequence space, unlike traditional </span>
<span>diffusion models that operate in a latent space. This design choice is justified by the fact that iterative </span>
<span>denoising in the latent space can lead to error accumulation, which is amplified when aligning </span>
<span>the latent space back to the actual sequence space.</span></p>
</section>
<section id="3-Improved-Classifier-Free-Guidance">

    <h3>
    <a href="#3-Improved-Classifier-Free-Guidance"><span>3. Improved Classifier-Free Guidance</span> </a>
    </h3>
<p><span>The UTSD model uses an improved classifier-free guidance strategy to ensure strong generalization ability. This strategy leverages the multi-scale </span>
<span>representation captured by the Condition Net as a conditional variable to guide the reconstruction of the forecast from Gaussian noise.</span></p>
</section>
<section id="Additional-Architectural-Components">

    <h3>
    <a href="#Additional-Architectural-Components"><span>Additional Architectural Components</span> </a>
    </h3>
<ul>
<li>
<strong><strong><span>Transfer-Adapter Module:</span></strong></strong><span> This module allows for efficient fine-tuning of the pre-trained UTSD model on specific downstream tasks. It transforms </span>
<span>the fluctuation patterns learned during pre-training into the latent space of the target domain, enabling the generation of domain-specific time series samples.</span>
</li>
<li>
<strong><strong><span>Blocks Implementation:</span></strong></strong><span> The Condition Net and Denoising Net are built using ResNet1D and Transformer1D modules. The ResNet1D modules handle </span>
<span>the embedding of diffusion timesteps and latent representations, while the Transformer1D modules capture dependencies within the observation sequence and leverage historical fluctuation patterns as context for denoising.</span>
</li>
</ul>
</section>
</section>
]]></content>
</entry>

<entry>
<title type="text">Flow Matching</title>
<link href="https://mbottoni.github.io/2024/12/01/flow.html" rel="alternate" type="text/html" title="Flow Matching" />
<published>2024-12-01T00:00:00+00:00</published>
<updated>2024-12-01T00:00:00+00:00</updated>
<id>https://mbottoni.github.io/2024/12/01/flow</id>
<author><name>Alex Kladov</name></author>
<summary type="html"><![CDATA[In summary, flow matching is a generative modeling technique that provides 
an elegant way to transform data distributions.]]></summary>
<content type="html" xml:base="https://mbottoni.github.io/2024/12/01/flow.html"><![CDATA[
    <h1>
    <a href="#Flow-Matching"><span>Flow Matching</span> <time datetime="2024-12-01">Dec 1, 2024</time></a>
    </h1>

<figure>

<img alt="" src="/assets/flow.png">
</figure>
<p><span>In summary, flow matching is a generative modeling technique that provides </span>
<span>an elegant way to transform data distributions.</span></p>

<figure class="code-block">


<pre><code><span class="line"></span>
<span class="line"><span class="hl-keyword">import</span> numpy <span class="hl-keyword">as</span> np</span>
<span class="line"><span class="hl-keyword">import</span> torch</span>
<span class="line"><span class="hl-keyword">import</span> torch.nn <span class="hl-keyword">as</span> nn</span>
<span class="line"><span class="hl-keyword">import</span> torch.optim <span class="hl-keyword">as</span> optim</span>
<span class="line"></span>
<span class="line"><span class="hl-keyword">class</span> <span class="hl-title class_">FlowMatchingModel</span>:</span>
<span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">__init__</span>(<span class="hl-params">self, input_dim, hidden_dim</span>):</span>
<span class="line">        <span class="hl-string">&quot;&quot;&quot;</span></span>
<span class="line"><span class="hl-string">        Initialize Flow Matching Model</span></span>
<span class="line"><span class="hl-string">        </span></span>
<span class="line"><span class="hl-string">        Args:</span></span>
<span class="line"><span class="hl-string">            input_dim (int): Dimension of input data</span></span>
<span class="line"><span class="hl-string">            hidden_dim (int): Dimension of hidden layers</span></span>
<span class="line"><span class="hl-string">        &quot;&quot;&quot;</span></span>
<span class="line">        self.base_distribution = <span class="hl-literal">None</span>  <span class="hl-comment"># Initial data distribution</span></span>
<span class="line">        self.target_distribution = <span class="hl-literal">None</span>  <span class="hl-comment"># Target data distribution</span></span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Neural network to learn the flow</span></span>
<span class="line">        self.flow_network = nn.Sequential(</span>
<span class="line">            nn.Linear(input_dim + <span class="hl-number">1</span>, hidden_dim),  <span class="hl-comment"># +1 for time conditioning</span></span>
<span class="line">            nn.ReLU(),</span>
<span class="line">            nn.Linear(hidden_dim, hidden_dim),</span>
<span class="line">            nn.ReLU(),</span>
<span class="line">            nn.Linear(hidden_dim, input_dim)</span>
<span class="line">        )</span>
<span class="line">        </span>
<span class="line">        self.optimizer = optim.Adam(self.flow_network.parameters())</span>
<span class="line">    </span>
<span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">sample_base_distribution</span>(<span class="hl-params">self, num_samples</span>):</span>
<span class="line">        <span class="hl-string">&quot;&quot;&quot;</span></span>
<span class="line"><span class="hl-string">        Sample from the base (initial) distribution</span></span>
<span class="line"><span class="hl-string">        </span></span>
<span class="line"><span class="hl-string">        Args:</span></span>
<span class="line"><span class="hl-string">            num_samples (int): Number of samples to generate</span></span>
<span class="line"><span class="hl-string">        </span></span>
<span class="line"><span class="hl-string">        Returns:</span></span>
<span class="line"><span class="hl-string">            torch.Tensor: Samples from base distribution</span></span>
<span class="line"><span class="hl-string">        &quot;&quot;&quot;</span></span>
<span class="line">        <span class="hl-comment"># Example: Gaussian distribution</span></span>
<span class="line">        <span class="hl-keyword">return</span> torch.randn(num_samples, self.input_dim)</span>
<span class="line">    </span>
<span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">probability_flow_ode</span>(<span class="hl-params">self, x, t</span>):</span>
<span class="line">        <span class="hl-string">&quot;&quot;&quot;</span></span>
<span class="line"><span class="hl-string">        Compute the probability flow ODE</span></span>
<span class="line"><span class="hl-string">        </span></span>
<span class="line"><span class="hl-string">        Args:</span></span>
<span class="line"><span class="hl-string">            x (torch.Tensor): Current data point</span></span>
<span class="line"><span class="hl-string">            t (torch.Tensor): Time variable</span></span>
<span class="line"><span class="hl-string">        </span></span>
<span class="line"><span class="hl-string">        Returns:</span></span>
<span class="line"><span class="hl-string">            torch.Tensor: Flow direction</span></span>
<span class="line"><span class="hl-string">        &quot;&quot;&quot;</span></span>
<span class="line">        <span class="hl-comment"># Combine input and time as network input</span></span>
<span class="line">        network_input = torch.cat([x, t], dim=<span class="hl-number">1</span>)</span>
<span class="line">        <span class="hl-keyword">return</span> self.flow_network(network_input)</span>
<span class="line">    </span>
<span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">conditional_vector_field</span>(<span class="hl-params">self, x0, x1, t</span>):</span>
<span class="line">        <span class="hl-string">&quot;&quot;&quot;</span></span>
<span class="line"><span class="hl-string">        Compute the conditional vector field</span></span>
<span class="line"><span class="hl-string">        </span></span>
<span class="line"><span class="hl-string">        Args:</span></span>
<span class="line"><span class="hl-string">            x0 (torch.Tensor): Initial data point</span></span>
<span class="line"><span class="hl-string">            x1 (torch.Tensor): Target data point</span></span>
<span class="line"><span class="hl-string">            t (torch.Tensor): Time variable</span></span>
<span class="line"><span class="hl-string">        </span></span>
<span class="line"><span class="hl-string">        Returns:</span></span>
<span class="line"><span class="hl-string">            torch.Tensor: Conditional vector field</span></span>
<span class="line"><span class="hl-string">        &quot;&quot;&quot;</span></span>
<span class="line">        <span class="hl-comment"># Interpolate between source and target</span></span>
<span class="line">        x_t = x0 * (<span class="hl-number">1</span> - t) + x1 * t</span>
<span class="line">        vector_field = x1 - x0</span>
<span class="line">        <span class="hl-keyword">return</span> vector_field</span>
<span class="line">    </span>
<span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">loss_function</span>(<span class="hl-params">self, x0, x1</span>):</span>
<span class="line">        <span class="hl-string">&quot;&quot;&quot;</span></span>
<span class="line"><span class="hl-string">        Compute the flow matching loss</span></span>
<span class="line"><span class="hl-string">        </span></span>
<span class="line"><span class="hl-string">        Args:</span></span>
<span class="line"><span class="hl-string">            x0 (torch.Tensor): Initial data points</span></span>
<span class="line"><span class="hl-string">            x1 (torch.Tensor): Target data points</span></span>
<span class="line"><span class="hl-string">        </span></span>
<span class="line"><span class="hl-string">        Returns:</span></span>
<span class="line"><span class="hl-string">            torch.Tensor: Training loss</span></span>
<span class="line"><span class="hl-string">        &quot;&quot;&quot;</span></span>
<span class="line">        batch_size = x0.shape[<span class="hl-number">0</span>]</span>
<span class="line">        t = torch.rand(batch_size, <span class="hl-number">1</span>)  <span class="hl-comment"># Random time sampling</span></span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Compute vector field</span></span>
<span class="line">        true_vector_field = self.conditional_vector_field(x0, x1, t)</span>
<span class="line">        predicted_vector_field = self.probability_flow_ode(x0, t)</span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Compute MSE loss between true and predicted vector fields</span></span>
<span class="line">        loss = torch.mean((predicted_vector_field - true_vector_field) ** <span class="hl-number">2</span>)</span>
<span class="line">        <span class="hl-keyword">return</span> loss</span>
<span class="line">    </span>
<span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">train</span>(<span class="hl-params">self, dataloader, epochs</span>):</span>
<span class="line">        <span class="hl-string">&quot;&quot;&quot;</span></span>
<span class="line"><span class="hl-string">        Train the Flow Matching Model</span></span>
<span class="line"><span class="hl-string">        </span></span>
<span class="line"><span class="hl-string">        Args:</span></span>
<span class="line"><span class="hl-string">            dataloader (torch.utils.data.DataLoader): Training data</span></span>
<span class="line"><span class="hl-string">            epochs (int): Number of training epochs</span></span>
<span class="line"><span class="hl-string">        &quot;&quot;&quot;</span></span>
<span class="line">        <span class="hl-keyword">for</span> epoch <span class="hl-keyword">in</span> <span class="hl-built_in">range</span>(epochs):</span>
<span class="line">            <span class="hl-keyword">for</span> batch_x0, batch_x1 <span class="hl-keyword">in</span> dataloader:</span>
<span class="line">                self.optimizer.zero_grad()</span>
<span class="line">                loss = self.loss_function(batch_x0, batch_x1)</span>
<span class="line">                loss.backward()</span>
<span class="line">                self.optimizer.step()</span>
<span class="line">            </span>
<span class="line">            <span class="hl-built_in">print</span>(<span class="hl-string">f&quot;Epoch <span class="hl-subst">{epoch+<span class="hl-number">1</span>}</span>/<span class="hl-subst">{epochs}</span>, Loss: <span class="hl-subst">{loss.item()}</span>&quot;</span>)</span>
<span class="line">    </span>
<span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">generate_samples</span>(<span class="hl-params">self, num_samples</span>):</span>
<span class="line">        <span class="hl-string">&quot;&quot;&quot;</span></span>
<span class="line"><span class="hl-string">        Generate new samples using the learned flow</span></span>
<span class="line"><span class="hl-string">        </span></span>
<span class="line"><span class="hl-string">        Args:</span></span>
<span class="line"><span class="hl-string">            num_samples (int): Number of samples to generate</span></span>
<span class="line"><span class="hl-string">        </span></span>
<span class="line"><span class="hl-string">        Returns:</span></span>
<span class="line"><span class="hl-string">            torch.Tensor: Generated samples</span></span>
<span class="line"><span class="hl-string">        &quot;&quot;&quot;</span></span>
<span class="line">        <span class="hl-comment"># Start from base distribution and follow the learned flow</span></span>
<span class="line">        x0 = self.sample_base_distribution(num_samples)</span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Perform sampling through ODE solving</span></span>
<span class="line">        x_generated = x0  <span class="hl-comment"># Starting point</span></span>
<span class="line">        time_steps = torch.linspace(<span class="hl-number">0</span>, <span class="hl-number">1</span>, <span class="hl-number">100</span>)</span>
<span class="line">        </span>
<span class="line">        <span class="hl-keyword">for</span> t <span class="hl-keyword">in</span> time_steps[<span class="hl-number">1</span>:]:</span>
<span class="line">            vector_field = self.probability_flow_ode(x_generated, t)</span>
<span class="line">            x_generated += vector_field * (time_steps[<span class="hl-number">1</span>] - time_steps[<span class="hl-number">0</span>])</span>
<span class="line">        </span>
<span class="line">        <span class="hl-keyword">return</span> x_generated</span>
<span class="line"></span>
<span class="line"><span class="hl-keyword">def</span> <span class="hl-title function_">main</span>():</span>
<span class="line">    <span class="hl-comment"># Hyperparameters</span></span>
<span class="line">    input_dim = <span class="hl-number">10</span></span>
<span class="line">    hidden_dim = <span class="hl-number">64</span></span>
<span class="line">    num_epochs = <span class="hl-number">100</span></span>
<span class="line">    </span>
<span class="line">    flow_matching = FlowMatchingModel(input_dim, hidden_dim)</span>
<span class="line">    flow_matching.train(dataloader, num_epochs)</span>
<span class="line">    generated_samples = flow_matching.generate_samples(<span class="hl-number">1000</span>)</span></code></pre>

</figure>
]]></content>
</entry>

<entry>
<title type="text">Scarf: Self Supervised Learning for Tabular Data</title>
<link href="https://mbottoni.github.io/2024/11/25/ssl.html" rel="alternate" type="text/html" title="Scarf: Self Supervised Learning for Tabular Data" />
<published>2024-11-25T00:00:00+00:00</published>
<updated>2024-11-25T00:00:00+00:00</updated>
<id>https://mbottoni.github.io/2024/11/25/ssl</id>
<author><name>Alex Kladov</name></author>
<summary type="html"><![CDATA[Machine learning often struggles with the scarcity of labeled data. While unlabeled datasets 
are abundant, obtaining high-quality labeled data remains expensive and time-consuming. SCARF 
emerges as a breakthrough methodology that transforms how we extract meaningful 
representations from raw, untagged information.]]></summary>
<content type="html" xml:base="https://mbottoni.github.io/2024/11/25/ssl.html"><![CDATA[
    <h1>
    <a href="#Scarf-Self-Supervised-Learning-for-Tabular-Data"><span>Scarf: Self Supervised Learning for Tabular Data</span> <time datetime="2024-11-25">Nov 25, 2024</time></a>
    </h1>

<figure>

<img alt="" src="/assets/scarf.png">
</figure>
<p><span>Machine learning often struggles with the scarcity of labeled data. While unlabeled datasets </span>
<span>are abundant, obtaining high-quality labeled data remains expensive and time-consuming. SCARF </span>
<span>emerges as a breakthrough methodology that transforms how we extract meaningful </span>
<span>representations from raw, untagged information.</span></p>
<section id="Innovation">

    <h2>
    <a href="#Innovation"><span>Innovation</span> </a>
    </h2>
<p><span>SCARF introduces a novel approach to generating data views by strategically corrupting </span>
<span>input features. Unlike domain-specific augmentation techniques, this method works universally across datasets:</span></p>
<ul>
<li>
<span>Randomly select a subset of features</span>
</li>
<li>
<span>Replace selected features with samples from their empirical marginal distributions</span>
</li>
<li>
<span>Leverage contrastive learning to maximize representation quality</span>
</li>
</ul>
</section>
<section id="Technical-Mechanism">

    <h2>
    <a href="#Technical-Mechanism"><span>Technical Mechanism</span> </a>
    </h2>
<p><span>The approach employs:</span></p>
<ul>
<li>
<span>An encoder network (f)</span>
</li>
<li>
<span>A pre-training head network (g)</span>
</li>
<li>
<span>InfoNCE contrastive loss function</span>
</li>
</ul>
<p><span>The core objective is generating representations that maintain semantic integrity </span>
<span>while being robust to feature perturbations.</span></p>
<p><span>Tested across 72 real-world classification datasets, SCARF demonstrated significant improvements:</span></p>
<ul>
<li>
<span>Enhanced classification accuracy</span>
</li>
<li>
<span>Improved performance under label noise</span>
</li>
<li>
<span>Effective semi-supervised learning capabilities</span>
</li>
<li>
<span>Domain-agnostic representation learning</span>
</li>
<li>
<span>Minimal sensitivity to feature scaling</span>
</li>
<li>
<span>Hyperparameter stability</span>
</li>
<li>
<span>Compatibility with existing machine learning techniques</span>
</li>
</ul>
<p><span>SCARF represents a pivotal advancement in self-supervised learning, offering a flexible, powerful </span>
<span>approach to extracting meaningful representations with minimal labeled data.</span></p>
</section>
]]></content>
</entry>

<entry>
<title type="text">The Platonic Representation Hypothesis: Are AI Models Converging on Universal Representations?</title>
<link href="https://mbottoni.github.io/2024/11/17/plato.html" rel="alternate" type="text/html" title="The Platonic Representation Hypothesis: Are AI Models Converging on Universal Representations?" />
<published>2024-11-17T00:00:00+00:00</published>
<updated>2024-11-17T00:00:00+00:00</updated>
<id>https://mbottoni.github.io/2024/11/17/plato</id>
<author><name>Alex Kladov</name></author>
<summary type="html"><![CDATA[This blog post is based on The Platonic Representation Hypothesis by Huh et al. (2024), MIT. AI models are becoming increasingly similar 
in how they represent the world, regardless of how they're trained or what kind of data they process. This fascinating trend suggests we 
may be approaching a universal way of representing reality.]]></summary>
<content type="html" xml:base="https://mbottoni.github.io/2024/11/17/plato.html"><![CDATA[
    <h1>
    <a href="#The-Platonic-Representation-Hypothesis-Are-AI-Models-Converging-on-Universal-Representations"><span>The Platonic Representation Hypothesis: Are AI Models Converging on Universal Representations?</span> <time datetime="2024-11-17">Nov 17, 2024</time></a>
    </h1>

<figure>

<img alt="" src="/assets/plato.png">
</figure>
<p><span>This blog post is based on </span>&ldquo;<span>The Platonic Representation Hypothesis</span>&rdquo;<span> by Huh et al. (2024), MIT. AI models are becoming increasingly similar </span>
<span>in how they represent the world, regardless of how they</span>&rsquo;<span>re trained or what kind of data they process. This fascinating trend suggests we </span>
<span>may be approaching a universal way of representing reality.</span></p>
<section id="The-Evidence-for-Convergence">

    <h2>
    <a href="#The-Evidence-for-Convergence"><span>The Evidence for Convergence</span> </a>
    </h2>
<p><span>The researchers present several compelling pieces </span>
<span>of evidence that AI representations are converging:</span></p>
<ol>
<li>
<p><strong><strong><span>Cross-Model Alignment</span></strong></strong><span>: Different neural networks, even when trained on different tasks and datasets, increasingly represent </span>
<span>information in similar ways. As models get bigger and better, they tend to become more aligned with each other.</span></p>
</li>
<li>
<p><strong><strong><span>Cross-Modal Alignment</span></strong></strong><span>: Perhaps most surprisingly, language models and vision models are </span>
<span>developing similar internal representations. The better a language model gets at </span>
<span>processing text, the more its representations align with those of vision models, and vice versa.</span></p>
</li>
<li>
<p><strong><strong><span>Brain Alignment</span></strong></strong><span>: These AI representations are also increasingly aligning with </span>
<span>how biological brains process information, suggesting they may be discovering </span>
<span>fundamental principles about how to represent the world.</span></p>
</li>
</ol>
</section>
<section id="Why-Is-This-Happening">

    <h2>
    <a href="#Why-Is-This-Happening"><span>Why Is This Happening?</span> </a>
    </h2>
<p><span>The researchers propose three key mechanisms driving this convergence:</span></p>
<ol>
<li>
<p><strong><strong><span>Task Generality</span></strong></strong><span>: As models are trained on more diverse tasks, they</span>&rsquo;<span>re forced to develop </span>
<span>representations that capture fundamental aspects of reality that are useful across many different problems.</span></p>
</li>
<li>
<p><strong><strong><span>Model Capacity</span></strong></strong><span>: Larger models have more flexibility to find optimal </span>
<span>representations, making them more likely to converge on similar solutions.</span></p>
</li>
<li>
<p><strong><strong><span>Simplicity Bias</span></strong></strong><span>: Neural networks naturally prefer simpler solutions, and as models </span>
<span>get bigger, this bias toward simplicity actually gets stronger, pushing them toward shared, elegant representations.</span></p>
</li>
</ol>
</section>
<section id="What-Does-This-Mean">

    <h2>
    <a href="#What-Does-This-Mean"><span>What Does This Mean?</span> </a>
    </h2>
<p><span>If this hypothesis is correct, it has several important implications:</span></p>
<ul>
<li>
<span>Training data can be shared across modalities - images could help train better language models and vice versa</span>
</li>
<li>
<span>Translation between different modalities (like text-to-image or image-to-text) should become easier</span>
</li>
<li>
<span>As models scale up, they may naturally reduce problems like hallucination and bias</span>
</li>
<li>
<span>We may be approaching a fundamental computational representation of reality. Wow!</span>
</li>
</ul>
<p><span>This work suggests we may be witnessing the emergence of a universal way of representing knowledge - something akin </span>
<span>to Plato</span>&rsquo;<span>s concept of ideal forms, but discovered through machine learning.</span></p>
<p><span>The paper</span>&rsquo;<span>s central metaphor draws from Plato</span>&rsquo;<span>s Allegory of the Cave: just as Plato suggested there was an ideal reality behind </span>
<span>our sensory experiences, these researchers suggest there may be an ideal computational representation of reality that </span>
<span>different AI models are gradually discovering.</span></p>
</section>
]]></content>
</entry>

<entry>
<title type="text">Sparse Autoencoders</title>
<link href="https://mbottoni.github.io/2024/11/10/sae.html" rel="alternate" type="text/html" title="Sparse Autoencoders" />
<published>2024-11-10T00:00:00+00:00</published>
<updated>2024-11-10T00:00:00+00:00</updated>
<id>https://mbottoni.github.io/2024/11/10/sae</id>
<author><name>Alex Kladov</name></author>
<summary type="html"><![CDATA[Sparse autoencoders are neural networks that learn compressed representations of 
data while enforcing sparsity - a constraint that ensures most neurons 
remain inactive for any given input. This approach leads to more 
robust and interpretable features, often capturing meaningful patterns in the data.]]></summary>
<content type="html" xml:base="https://mbottoni.github.io/2024/11/10/sae.html"><![CDATA[
    <h1>
    <a href="#Sparse-Autoencoders"><span>Sparse Autoencoders</span> <time datetime="2024-11-10">Nov 10, 2024</time></a>
    </h1>
<p><span>Sparse autoencoders are neural networks that learn compressed representations of </span>
<span>data while enforcing sparsity - a constraint that ensures most neurons </span>
<span>remain inactive for any given input. This approach leads to more </span>
<span>robust and interpretable features, often capturing meaningful patterns in the data.</span></p>
<section id="Concepts">

    <h2>
    <a href="#Concepts"><span>Concepts</span> </a>
    </h2>
<p><span>The key idea behind sparse autoencoders is adding a sparsity penalty to the </span>
<span>standard autoencoder loss function. For a target sparsity level ρ (typically 0.05 or lower), we want </span>
<span>the average activation of each hidden neuron ρ̂ to be close to ρ. The network achieves this by minimizing two components:</span></p>
<ol>
<li>
<span>Reconstruction Error: How well the network reconstructs the input</span>
</li>
<li>
<span>Sparsity Penalty: How close the average activations are to the target sparsity</span>
</li>
</ol>
<p><span>Here</span>&rsquo;<span>s a simple pseudocode implementation to illustrate these concepts:</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">class</span> <span class="hl-title class_">SparseAutoencoder</span>:</span>
<span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">__init__</span>(<span class="hl-params">self, input_dim, hidden_dim, rho=<span class="hl-number">0.05</span>, beta=<span class="hl-number">1.0</span></span>):</span>
<span class="line">        <span class="hl-comment"># rho: target sparsity level</span></span>
<span class="line">        <span class="hl-comment"># beta: weight of sparsity penalty</span></span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Initialize weights and biases</span></span>
<span class="line">        self.W1 = random_matrix(input_dim, hidden_dim)</span>
<span class="line">        self.b1 = zeros(hidden_dim)</span>
<span class="line">        self.W2 = random_matrix(hidden_dim, input_dim)</span>
<span class="line">        self.b2 = zeros(input_dim)</span>
<span class="line"></span>
<span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">forward</span>(<span class="hl-params">self, X</span>):</span>
<span class="line">        <span class="hl-comment"># Encoder</span></span>
<span class="line">        hidden = sigmoid(dot(X, self.W1) + self.b1)</span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Decoder</span></span>
<span class="line">        output = sigmoid(dot(hidden, self.W2) + self.b2)</span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Calculate average activations for sparsity</span></span>
<span class="line">        rho_hat = mean(hidden, axis=<span class="hl-number">0</span>)</span>
<span class="line">        </span>
<span class="line">        <span class="hl-keyword">return</span> output, hidden, rho_hat</span>
<span class="line"></span>
<span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">loss</span>(<span class="hl-params">self, X, output, rho_hat</span>):</span>
<span class="line">        <span class="hl-comment"># Reconstruction error</span></span>
<span class="line">        reconstruction_loss = mean_squared_error(X, output)</span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Sparsity penalty (KL divergence)</span></span>
<span class="line">        sparsity_penalty = <span class="hl-built_in">sum</span>(</span>
<span class="line">            self.rho * log(self.rho / rho_hat) +</span>
<span class="line">            (<span class="hl-number">1</span> - self.rho) * log((<span class="hl-number">1</span> - self.rho) / (<span class="hl-number">1</span> - rho_hat))</span>
<span class="line">        )</span>
<span class="line">        </span>
<span class="line">        <span class="hl-keyword">return</span> reconstruction_loss + self.beta * sparsity_penalty</span></code></pre>

</figure>
<p><span>Let</span>&rsquo;<span>s look at a concrete example. Imagine we</span>&rsquo;<span>re processing MNIST digits:</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-comment"># Example usage with MNIST</span></span>
<span class="line">input_dim = <span class="hl-number">784</span>  <span class="hl-comment"># 28x28 pixels</span></span>
<span class="line">hidden_dim = <span class="hl-number">196</span>  <span class="hl-comment"># Compressed representation</span></span>
<span class="line">autoencoder = SparseAutoencoder(input_dim, hidden_dim)</span>
<span class="line"></span>
<span class="line"><span class="hl-comment"># Training loop example</span></span>
<span class="line"><span class="hl-keyword">for</span> epoch <span class="hl-keyword">in</span> <span class="hl-built_in">range</span>(num_epochs):</span>
<span class="line">    <span class="hl-keyword">for</span> batch <span class="hl-keyword">in</span> data_loader:</span>
<span class="line">        <span class="hl-comment"># Forward pass</span></span>
<span class="line">        output, hidden, rho_hat = autoencoder.forward(batch)</span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Calculate loss</span></span>
<span class="line">        loss = autoencoder.loss(batch, output, rho_hat)</span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Update weights using backpropagation</span></span>
<span class="line">        gradients = calculate_gradients(loss)</span>
<span class="line">        update_weights(gradients)</span></code></pre>

</figure>
</section>
<section id="Practical-Examples">

    <h2>
    <a href="#Practical-Examples"><span>Practical Examples</span> </a>
    </h2>
<p><span>Let</span>&rsquo;<span>s look at some typical activation patterns:</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-comment"># Dense (standard) autoencoder activation example</span></span>
<span class="line"><span class="hl-comment"># [0.6, 0.4, 0.5, 0.3, 0.7, 0.4, 0.6, 0.5]</span></span>
<span class="line"><span class="hl-comment"># Many neurons are active simultaneously</span></span>
<span class="line"></span>
<span class="line"><span class="hl-comment"># Sparse autoencoder activation example</span></span>
<span class="line"><span class="hl-comment"># [0.01, 0.8, 0.02, 0.01, 0.03, 0.9, 0.02, 0.01]</span></span>
<span class="line"><span class="hl-comment"># Only a few neurons are strongly active</span></span></code></pre>

</figure>
<p><span>Consider an image processing task:</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-comment"># Example: Processing a 32x32 image patch</span></span>
<span class="line">patch = load_image_patch(<span class="hl-number">32</span>, <span class="hl-number">32</span>)  <span class="hl-comment"># 1024 dimensions</span></span>
<span class="line"></span>
<span class="line"><span class="hl-comment"># Configure sparse autoencoder</span></span>
<span class="line">encoder = SparseAutoencoder(</span>
<span class="line">    input_dim=<span class="hl-number">1024</span>,</span>
<span class="line">    hidden_dim=<span class="hl-number">256</span>,  <span class="hl-comment"># 4x compression</span></span>
<span class="line">    rho=<span class="hl-number">0.05</span>,  <span class="hl-comment"># Expect 5% average activation</span></span>
<span class="line">    beta=<span class="hl-number">1.0</span></span>
<span class="line">)</span>
<span class="line"></span>
<span class="line"><span class="hl-comment"># Process the patch</span></span>
<span class="line">reconstructed, hidden_repr, activations = encoder.forward(patch)</span>
<span class="line"></span>
<span class="line"><span class="hl-comment"># Example hidden representation might look like:</span></span>
<span class="line"><span class="hl-comment"># Most values near 0, with few strong activations:</span></span>
<span class="line"><span class="hl-comment"># [0.01, 0.00, 0.87, 0.02, 0.00, 0.92, ...]</span></span></code></pre>

</figure>
</section>
<section id="Implementation-Tips">

    <h2>
    <a href="#Implementation-Tips"><span>Implementation Tips</span> </a>
    </h2>
<ol>
<li>
<strong><strong><span>Initialization</span></strong></strong><span>: Initialize weights using a normal distribution with small variance (e.g., 0.01) to avoid saturation:</span>
</li>
</ol>

<figure class="code-block">


<pre><code><span class="line">W = numpy.random.normal(<span class="hl-number">0</span>, <span class="hl-number">0.01</span>, (input_dim, hidden_dim))</span></code></pre>

</figure>
<ol start="2">
<li>
<strong><strong><span>Monitoring</span></strong></strong><span>: Track both reconstruction error and average activations:</span>
</li>
</ol>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">def</span> <span class="hl-title function_">monitor_sparsity</span>(<span class="hl-params">hidden_activations</span>):</span>
<span class="line">    avg_activation = numpy.mean(hidden_activations)</span>
<span class="line">    active_neurons = numpy.mean(hidden_activations &gt; <span class="hl-number">0.1</span>)</span>
<span class="line">    <span class="hl-built_in">print</span>(<span class="hl-string">f&quot;Average activation: <span class="hl-subst">{avg_activation:<span class="hl-number">.3</span>f}</span>&quot;</span>)</span>
<span class="line">    <span class="hl-built_in">print</span>(<span class="hl-string">f&quot;Proportion of active neurons: <span class="hl-subst">{active_neurons:<span class="hl-number">.3</span>f}</span>&quot;</span>)</span></code></pre>

</figure>
<ol start="3">
<li>
<strong><strong><span>Hyperparameter Selection</span></strong></strong><span>:</span>
</li>
</ol>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-comment"># Conservative starting points</span></span>
<span class="line">hyperparams = {</span>
<span class="line">    <span class="hl-string">&#x27;learning_rate&#x27;</span>: <span class="hl-number">0.001</span>,</span>
<span class="line">    <span class="hl-string">&#x27;rho&#x27;</span>: <span class="hl-number">0.05</span>,        <span class="hl-comment"># Target sparsity</span></span>
<span class="line">    <span class="hl-string">&#x27;beta&#x27;</span>: <span class="hl-number">1.0</span>,        <span class="hl-comment"># Sparsity weight</span></span>
<span class="line">    <span class="hl-string">&#x27;batch_size&#x27;</span>: <span class="hl-number">128</span>,</span>
<span class="line">    <span class="hl-string">&#x27;hidden_dim&#x27;</span>: input_dim // <span class="hl-number">4</span>  <span class="hl-comment"># 4x compression</span></span>
<span class="line">}</span></code></pre>

</figure>
<p><span>The power of sparse autoencoders lies in their ability to discover specialized feature </span>
<span>detectors. Each neuron becomes sensitive to specific patterns in the input </span>
<span>data, making the learned representations more interpretable and often more </span>
<span>useful for downstream tasks like classification or anomaly detection.</span></p>
<p><span>Remember that the sparsity constraint isn</span>&rsquo;<span>t about having fewer neurons, but rather </span>
<span>about having fewer neurons active at once. This mimics biological neural networks, where energy efficiency is </span>
<span>achieved through sparse activation patterns.</span></p>
</section>
]]></content>
</entry>

<entry>
<title type="text">Temporal Graph Networks: A Deep Dive into Dynamic Graph Learning</title>
<link href="https://mbottoni.github.io/2024/11/03/temporal-gnn.html" rel="alternate" type="text/html" title="Temporal Graph Networks: A Deep Dive into Dynamic Graph Learning" />
<published>2024-11-03T00:00:00+00:00</published>
<updated>2024-11-03T00:00:00+00:00</updated>
<id>https://mbottoni.github.io/2024/11/03/temporal-gnn</id>
<author><name>Alex Kladov</name></author>
<summary type="html"><![CDATA[Real-world networks are rarely static. Social networks evolve as users form 
new connections, financial networks change with each transaction, and biological 
networks transform as proteins interact. Traditional Graph Neural Networks (GNNs) weren't 
designed for this dynamism. Enter Temporal Graph Networks (TGNs), a powerful framework for learning on dynamic graphs.]]></summary>
<content type="html" xml:base="https://mbottoni.github.io/2024/11/03/temporal-gnn.html"><![CDATA[
    <h1>
    <a href="#Temporal-Graph-Networks-A-Deep-Dive-into-Dynamic-Graph-Learning"><span>Temporal Graph Networks: A Deep Dive into Dynamic Graph Learning</span> <time datetime="2024-11-03">Nov 3, 2024</time></a>
    </h1>
<p><span>Real-world networks are rarely static. Social networks evolve as users form </span>
<span>new connections, financial networks change with each transaction, and biological </span>
<span>networks transform as proteins interact. Traditional Graph Neural Networks (GNNs) weren</span>&rsquo;<span>t </span>
<span>designed for this dynamism. Enter Temporal Graph Networks (TGNs), a powerful framework for learning on dynamic graphs.</span></p>
<section id="Understanding-Dynamic-Graphs">

    <h2>
    <a href="#Understanding-Dynamic-Graphs"><span>Understanding Dynamic Graphs</span> </a>
    </h2>
<p><span>Before diving into TGNs, let</span>&rsquo;<span>s clarify what we mean by dynamic graphs. A temporal graph </span>
<span>can be represented as a sequence of time-stamped events: G = {x(t₁), x(t₂), </span>&hellip;<span>}. Each event can be:</span></p>
<ol>
<li>
<span>Node-wise: Adding/updating a node with features</span>
</li>
<li>
<span>Edge-wise: Creating an interaction between nodes</span>
</li>
</ol>
</section>
<section id="Core-Components-of-TGN">

    <h2>
    <a href="#Core-Components-of-TGN"><span>Core Components of TGN</span> </a>
    </h2>
<section id="Memory-Module">

    <h3>
    <a href="#Memory-Module"><span>Memory Module</span> </a>
    </h3>
<p><span>The memory module is TGN</span>&rsquo;<span>s secret weapon. Each node maintains a memory state </span>
<span>that captures its history of interactions. This memory gets updated with each </span>
<span>new event, allowing the network to learn long-term dependencies.</span></p>
</section>
<section id="Message-Function">

    <h3>
    <a href="#Message-Function"><span>Message Function</span> </a>
    </h3>
<p><span>When an interaction occurs between nodes, messages are computed </span>
<span>to update their memories. Here</span>&rsquo;<span>s how the message functions work:</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">class</span> <span class="hl-title class_">TemporalGraphNetwork</span>:</span>
<span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">__init__</span>(<span class="hl-params">self, node_features, edge_features, memory_dimension</span>):</span>
<span class="line">        self.node_features = node_features</span>
<span class="line">        self.edge_features = edge_features</span>
<span class="line">        self.memory = {node_id: np.zeros(memory_dimension) <span class="hl-keyword">for</span> node_id <span class="hl-keyword">in</span> node_features}</span>
<span class="line">        </span>
<span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">compute_messages</span>(<span class="hl-params">self, source, target, time, edge_features</span>):</span>
<span class="line">        <span class="hl-string">&quot;&quot;&quot;Compute messages for source and target nodes.&quot;&quot;&quot;</span></span>
<span class="line">        <span class="hl-comment"># Get current memory states</span></span>
<span class="line">        source_memory = self.memory[source]</span>
<span class="line">        target_memory = self.memory[target]</span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Compute time delta from last update</span></span>
<span class="line">        delta_time = time - self.last_update[source]</span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Compute messages for both nodes</span></span>
<span class="line">        source_message = self.message_function(</span>
<span class="line">            source_memory=source_memory,</span>
<span class="line">            target_memory=target_memory,</span>
<span class="line">            delta_time=delta_time,</span>
<span class="line">            edge_features=edge_features</span>
<span class="line">        )</span>
<span class="line">        </span>
<span class="line">        target_message = self.message_function(</span>
<span class="line">            source_memory=target_memory,</span>
<span class="line">            target_memory=source_memory,</span>
<span class="line">            delta_time=delta_time,</span>
<span class="line">            edge_features=edge_features</span>
<span class="line">        )</span>
<span class="line">        </span>
<span class="line">        <span class="hl-keyword">return</span> source_message, target_message</span>
<span class="line"></span>
<span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">message_function</span>(<span class="hl-params">self, source_memory, target_memory, delta_time, edge_features</span>):</span>
<span class="line">        <span class="hl-string">&quot;&quot;&quot;Simple concatenation-based message function.&quot;&quot;&quot;</span></span>
<span class="line">        <span class="hl-keyword">return</span> np.concatenate([</span>
<span class="line">            source_memory,</span>
<span class="line">            target_memory,</span>
<span class="line">            [delta_time],</span>
<span class="line">            edge_features</span>
<span class="line">        ])</span></code></pre>

</figure>
</section>
<section id="Message-Aggregator">

    <h3>
    <a href="#Message-Aggregator"><span>Message Aggregator</span> </a>
    </h3>
<p><span>When multiple events involve the same node in a batch, their </span>
<span>messages need to be aggregated:</span></p>

<figure class="code-block">


<pre><code><span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">aggregate_messages</span>(<span class="hl-params">self, messages, aggregation_type=<span class="hl-string">&quot;last&quot;</span></span>):</span>
<span class="line">        <span class="hl-string">&quot;&quot;&quot;Aggregate multiple messages for the same node.&quot;&quot;&quot;</span></span>
<span class="line">        <span class="hl-keyword">if</span> aggregation_type == <span class="hl-string">&quot;last&quot;</span>:</span>
<span class="line">            <span class="hl-keyword">return</span> messages[-<span class="hl-number">1</span>]  <span class="hl-comment"># Return most recent message</span></span>
<span class="line">        <span class="hl-keyword">elif</span> aggregation_type == <span class="hl-string">&quot;mean&quot;</span>:</span>
<span class="line">            <span class="hl-keyword">return</span> np.mean(messages, axis=<span class="hl-number">0</span>)  <span class="hl-comment"># Average all messages</span></span></code></pre>

</figure>
</section>
<section id="Memory-Updater">

    <h3>
    <a href="#Memory-Updater"><span>Memory Updater</span> </a>
    </h3>
<p><span>The memory updater is typically implemented using a GRU or LSTM to update </span>
<span>node memories based on aggregated messages:</span></p>

<figure class="code-block">


<pre><code><span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">update_memory</span>(<span class="hl-params">self, node_id, message, time</span>):</span>
<span class="line">        <span class="hl-string">&quot;&quot;&quot;Update node memory using GRU-like update.&quot;&quot;&quot;</span></span>
<span class="line">        current_memory = self.memory[node_id]</span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># GRU-like update equations</span></span>
<span class="line">        update_gate = sigmoid(</span>
<span class="line">            self.W_update @ np.concatenate([current_memory, message])</span>
<span class="line">        )</span>
<span class="line">        reset_gate = sigmoid(</span>
<span class="line">            self.W_reset @ np.concatenate([current_memory, message])</span>
<span class="line">        )</span>
<span class="line">        </span>
<span class="line">        candidate_memory = tanh(</span>
<span class="line">            self.W_candidate @ np.concatenate([</span>
<span class="line">                reset_gate * current_memory,</span>
<span class="line">                message</span>
<span class="line">            ])</span>
<span class="line">        )</span>
<span class="line">        </span>
<span class="line">        <span class="hl-comment"># Update memory</span></span>
<span class="line">        self.memory[node_id] = (</span>
<span class="line">            update_gate * current_memory +</span>
<span class="line">            (<span class="hl-number">1</span> - update_gate) * candidate_memory</span>
<span class="line">        )</span>
<span class="line">        self.last_update[node_id] = time</span></code></pre>

</figure>
</section>
<section id="Embedding-Module">

    <h3>
    <a href="#Embedding-Module"><span>Embedding Module</span> </a>
    </h3>
<p><span>The embedding module generates node embeddings using the current memory </span>
<span>state and graph structure:</span></p>

<figure class="code-block">


<pre><code><span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">compute_embedding</span>(<span class="hl-params">self, node_id, time, num_layers=<span class="hl-number">1</span></span>):</span>
<span class="line">        <span class="hl-string">&quot;&quot;&quot;Compute node embedding using temporal graph attention.&quot;&quot;&quot;</span></span>
<span class="line">        current_embedding = self.memory[node_id]</span>
<span class="line">        </span>
<span class="line">        <span class="hl-keyword">for</span> layer <span class="hl-keyword">in</span> <span class="hl-built_in">range</span>(num_layers):</span>
<span class="line">            <span class="hl-comment"># Get temporal neighbors</span></span>
<span class="line">            neighbors = self.get_temporal_neighbors(node_id, time)</span>
<span class="line">            </span>
<span class="line">            <span class="hl-comment"># Compute attention scores</span></span>
<span class="line">            attention_scores = self.compute_temporal_attention(</span>
<span class="line">                query=current_embedding,</span>
<span class="line">                keys=[self.memory[n] <span class="hl-keyword">for</span> n <span class="hl-keyword">in</span> neighbors],</span>
<span class="line">                times=[self.last_update[n] <span class="hl-keyword">for</span> n <span class="hl-keyword">in</span> neighbors]</span>
<span class="line">            )</span>
<span class="line">            </span>
<span class="line">            <span class="hl-comment"># Aggregate neighbor information</span></span>
<span class="line">            neighbor_info = <span class="hl-built_in">sum</span>(</span>
<span class="line">                score * self.memory[neighbor]</span>
<span class="line">                <span class="hl-keyword">for</span> score, neighbor <span class="hl-keyword">in</span> <span class="hl-built_in">zip</span>(attention_scores, neighbors)</span>
<span class="line">            )</span>
<span class="line">            </span>
<span class="line">            <span class="hl-comment"># Update embedding</span></span>
<span class="line">            current_embedding = self.embedding_update(</span>
<span class="line">                current_embedding,</span>
<span class="line">                neighbor_info</span>
<span class="line">            )</span>
<span class="line">            </span>
<span class="line">        <span class="hl-keyword">return</span> current_embedding</span></code></pre>

</figure>
</section>
</section>
<section id="Training-Process">

    <h2>
    <a href="#Training-Process"><span>Training Process</span> </a>
    </h2>
<p><span>Training TGN requires careful handling of temporal dependencies. Here</span>&rsquo;<span>s the main training loop:</span></p>

<figure class="code-block">


<pre><code><span class="line">    <span class="hl-keyword">def</span> <span class="hl-title function_">train</span>(<span class="hl-params">self, temporal_edges, batch_size=<span class="hl-number">200</span></span>):</span>
<span class="line">        <span class="hl-string">&quot;&quot;&quot;Train the model on temporal edges.&quot;&quot;&quot;</span></span>
<span class="line">        message_store = {}  <span class="hl-comment"># Store for raw messages</span></span>
<span class="line">        </span>
<span class="line">        <span class="hl-keyword">for</span> batch <span class="hl-keyword">in</span> create_batches(temporal_edges, batch_size):</span>
<span class="line">            <span class="hl-comment"># 1. Update memories using stored messages</span></span>
<span class="line">            self.process_message_store(message_store)</span>
<span class="line">            </span>
<span class="line">            <span class="hl-comment"># 2. Compute embeddings</span></span>
<span class="line">            source_embeddings = [</span>
<span class="line">                self.compute_embedding(edge.source, edge.time)</span>
<span class="line">                <span class="hl-keyword">for</span> edge <span class="hl-keyword">in</span> batch</span>
<span class="line">            ]</span>
<span class="line">            target_embeddings = [</span>
<span class="line">                self.compute_embedding(edge.target, edge.time)</span>
<span class="line">                <span class="hl-keyword">for</span> edge <span class="hl-keyword">in</span> batch</span>
<span class="line">            ]</span>
<span class="line">            </span>
<span class="line">            <span class="hl-comment"># 3. Compute loss</span></span>
<span class="line">            loss = self.compute_loss(</span>
<span class="line">                source_embeddings,</span>
<span class="line">                target_embeddings,</span>
<span class="line">                batch.labels</span>
<span class="line">            )</span>
<span class="line">            </span>
<span class="line">            <span class="hl-comment"># 4. Backward pass</span></span>
<span class="line">            loss.backward()</span>
<span class="line">            </span>
<span class="line">            <span class="hl-comment"># 5. Store raw messages for next batch</span></span>
<span class="line">            <span class="hl-keyword">for</span> edge <span class="hl-keyword">in</span> batch:</span>
<span class="line">                message_store[edge.source] = (</span>
<span class="line">                    edge.source,</span>
<span class="line">                    edge.target,</span>
<span class="line">                    edge.time,</span>
<span class="line">                    edge.features</span>
<span class="line">                )</span>
<span class="line">                message_store[edge.target] = (</span>
<span class="line">                    edge.target,</span>
<span class="line">                    edge.source,</span>
<span class="line">                    edge.time,</span>
<span class="line">                    edge.features</span>
<span class="line">                )</span></code></pre>

</figure>
</section>
<section id="Key-Advantages-and-applications">

    <h2>
    <a href="#Key-Advantages-and-applications"><span>Key Advantages and applications</span> </a>
    </h2>
<ol>
<li>
<strong><strong><span>Memory Efficiency</span></strong></strong><span>: TGN maintains a compact memory state for each node instead of storing the entire history.</span>
</li>
<li>
<strong><strong><span>Temporal Awareness</span></strong></strong><span>: The framework naturally handles time-stamped events and evolving graphs.</span>
</li>
<li>
<strong><strong><span>Flexibility</span></strong></strong><span>: Different message, aggregation, and memory update functions can be used based on the specific application.</span>
</li>
<li>
<strong><strong><span>Scalability</span></strong></strong><span>: The batched training process allows for efficient processing of large temporal graphs.</span>
</li>
</ol>
<p><span>TGNs have shown remarkable success in various domains:</span>
<span>- Dynamic link prediction in social networks</span>
<span>- User-item interaction modeling in recommender systems</span>
<span>- Temporal knowledge graph completion</span>
<span>- Financial fraud detection</span>
<span>- Traffic prediction</span></p>
</section>
<section id="Implementation-Considerations">

    <h2>
    <a href="#Implementation-Considerations"><span>Implementation Considerations</span> </a>
    </h2>
<ul>
<li>
<strong><strong><span>Batch Size</span></strong></strong><span>: Smaller batches ensure more frequent memory updates but slower training.</span>
</li>
<li>
<strong><strong><span>Memory Dimension</span></strong></strong><span>: Larger memory can capture more complex patterns but requires more computation.</span>
</li>
<li>
<strong><strong><span>Neighbor Sampling</span></strong></strong><span>: Sampling recent neighbors often works better than uniform sampling.</span>
</li>
<li>
<strong><strong><span>Time Encoding</span></strong></strong><span>: Different time encoding strategies can be used based on the temporal patterns in the data.</span>
</li>
</ul>
</section>
]]></content>
</entry>

<entry>
<title type="text">Graph Convolutional Networks</title>
<link href="https://mbottoni.github.io/2024/10/26/gcn.html" rel="alternate" type="text/html" title="Graph Convolutional Networks" />
<published>2024-10-26T00:00:00+00:00</published>
<updated>2024-10-26T00:00:00+00:00</updated>
<id>https://mbottoni.github.io/2024/10/26/gcn</id>
<author><name>Alex Kladov</name></author>
<summary type="html"><![CDATA[Graph Convolutional Networks extend deep learning to graph-structured data by 
generalizing the convolution operation from regular grids (like images) to 
irregular graph domains. The key innovation lies in how they aggregate and transform 
feature information from a node's local neighborhood through spectral or spatial convolutions.
In GCNs, each layer performs neighborhood feature aggregation where a node's representation is 
updated by combining its features with those of its 
adjacent nodes. This aggregation is typically followed by a non-linear 
transformation. The process can be mathematically represented as a function of 
the normalized adjacency matrix, feature matrix, and learned weight matrices.
Here's the pseudocode for a basic GCN layer:]]></summary>
<content type="html" xml:base="https://mbottoni.github.io/2024/10/26/gcn.html"><![CDATA[
    <h1>
    <a href="#Graph-Convolutional-Networks"><span>Graph Convolutional Networks</span> <time datetime="2024-10-26">Oct 26, 2024</time></a>
    </h1>

<figure>

<img alt="" src="/assets/gcn.png">
</figure>
<p><span>Graph Convolutional Networks extend deep learning to graph-structured data by </span>
<span>generalizing the convolution operation from regular grids (like images) to </span>
<span>irregular graph domains. The key innovation lies in how they aggregate and transform </span>
<span>feature information from a node</span>&rsquo;<span>s local neighborhood through spectral or spatial convolutions.</span>
<span>In GCNs, each layer performs neighborhood feature aggregation where a node</span>&rsquo;<span>s representation is </span>
<span>updated by combining its features with those of its </span>
<span>adjacent nodes. This aggregation is typically followed by a non-linear </span>
<span>transformation. The process can be mathematically represented as a function of </span>
<span>the normalized adjacency matrix, feature matrix, and learned weight matrices.</span>
<span>Here</span>&rsquo;<span>s the pseudocode for a basic GCN layer:</span></p>

<figure class="code-block">


<pre><code><span class="line"><span class="hl-keyword">def</span> <span class="hl-title function_">GCN_layer</span>(<span class="hl-params">adjacency_matrix A, feature_matrix X, weight_matrix W</span>):</span>
<span class="line">    <span class="hl-comment"># Add self-loops to adjacency matrix</span></span>
<span class="line">    A_hat = A + identity_matrix(A.shape[<span class="hl-number">0</span>])</span>
<span class="line">    </span>
<span class="line">    <span class="hl-comment"># Compute degree matrix</span></span>
<span class="line">    D = diagonal_matrix([<span class="hl-built_in">sum</span>(row) <span class="hl-keyword">for</span> row <span class="hl-keyword">in</span> A_hat])</span>
<span class="line">    </span>
<span class="line">    <span class="hl-comment"># Symmetric normalization</span></span>
<span class="line">    D_inv_sqrt = inverse_sqrt(D)</span>
<span class="line">    A_norm = D_inv_sqrt @ A_hat @ D_inv_sqrt</span>
<span class="line">    </span>
<span class="line">    <span class="hl-comment"># Feature transformation and aggregation</span></span>
<span class="line">    H = relu(A_norm @ X @ W)</span>
<span class="line">    </span>
<span class="line">    <span class="hl-keyword">return</span> H</span>
<span class="line"></span>
<span class="line"><span class="hl-keyword">def</span> <span class="hl-title function_">GCN_forward</span>(<span class="hl-params">G, X</span>):</span>
<span class="line">    <span class="hl-comment"># Initialize learnable weight matrices</span></span>
<span class="line">    W1 = initialize_weights(input_dim, hidden_dim)</span>
<span class="line">    W2 = initialize_weights(hidden_dim, output_dim)</span>
<span class="line">    </span>
<span class="line">    <span class="hl-comment"># First GCN layer</span></span>
<span class="line">    H1 = GCN_layer(G.adjacency, X, W1)</span>
<span class="line">    </span>
<span class="line">    <span class="hl-comment"># Second GCN layer</span></span>
<span class="line">    H2 = GCN_layer(G.adjacency, H1, W2)</span>
<span class="line">    </span>
<span class="line">    <span class="hl-comment"># Final node representations</span></span>
<span class="line">    <span class="hl-keyword">return</span> H2</span></code></pre>

</figure>
<p><span>The core operation performs message passing </span>
<span>through Â = D</span><sup><span>(-1/2)(A + I)D</span></sup><span>(-1/2), where A is the adjacency matrix, I is the identity </span>
<span>matrix (self-loops), and D is the degree matrix. This normalization prevents numerical </span>
<span>instabilities and exploding/vanishing gradients. Each layer then applies this </span>
<span>normalized adjacency matrix to the node features, followed by a learned weight </span>
<span>transformation and non-linearity.</span></p>
]]></content>
</entry>

</feed>
